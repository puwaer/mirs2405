/*void appendToArray(int **A, int *array_size, int n) {
  // 新しいサイズのためにメモリを再割り当て
  *A = realloc(*A, (*array_size + 1) * sizeof(int));
    
  if (*A != NULL) {
    // 配列の末尾に新しい要素を追加
    (*A)[*array_size] = n;
    // サイズを1つ増やす
    (*array_size)++;
    }   
}*/
