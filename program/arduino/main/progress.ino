/*できてない関数*/

// confirmation_device() 各デバイスの認識
// arm(&arm_angle1, &arm_angle2, &arm_angle3, &arm_angle4, &arm_gripper)　ロボットアーム制御
// run_ToPerson(&x_coordinate, &y_coordinate)　認識したヒトを追尾
// hand_check() ものを持っているか確認
// run_ToHome()　定位置へ戻る
// item_get()　armに配達物を補充
// arm_open() アームのモータの初期処理

//各種割り込み処理
